cbfc -- cburggie's brainfuck compiler

about:
	cbfc compiles brainf_ck source to C code. You can then use any c compiler
	to create a native binary. Code compiled with cbfc is accomplised through
	stdin and stdout exclusively, so pipe your input or output if you need to.

build:
	$ make

install:
	$ make install

use:
	$ cbfc [output_filename.bf.c] input_filename.bf

